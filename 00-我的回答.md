
```csharp
SkillManager {
    CastSkill
    IsSkillUnlocked
}

SoundManager {
    PlaySkillSound
}

AnimationManager {
    PlaySkillAnimation
}

ConfigManager{
    SaveSkillConfig // 进行技能配置表获取
}
```

## 开闭原则
1. 对修改开放 对扩展开放 解决方法 
    - 通过接口抽象出 不同类型的 GetArea 传入接口
    - 通过 字典根据类型去进行不同 的方法实现 但是还是要对字典数据改变
2. 修改的的化可能导致范围判断的不准确 使用多层if else 很可能存在着 判断相互以来的情况

interface ICalcGold{
    public int CalcGold();
}

//Normal
public class NormalCalcGold :ICalcGold{
    public int CalcGold() =>  10;
}

//Elite
public class EliteCalcGold :ICalcGold{
    public int CalcGold() => 50 + Random.Range(0, 20);;
}
//Boss
public class BossCalcGold :ICalcGold{
    public int CalcGold() => 200 + Random.Range(50, 100);
}

## 里氏替换
1. 修改了伤害的传播路径应该在 GetEffectValue 处理伤害 实际是在 AddStack 爆炸伤害计算
2. 父类允许的行为在子类中被拒绝了 

```cs
enum ItemType{
    _1,
    _2,
}
public class Item
{
    protected int durability;
    protected ItemType _type

    public virtual bool Repair(int amount)
    {
        if( _type != _2)
            durability = Math.Min(durability + amount, MaxDurability);
            return true;

        return false;
    }
}

public class IndestructibleItem : Item
{
    public override bool Repair(int amount)
    {
        super.Repair(amount)
    }
}
```


## 接口隔离
1. 被迫实现不属于他的行为，甚至会触发bug导致角色进行与职业不相符合的行为
2. ICharacterAction 定义了过多的行为 接口的定义过于宽泛 要进行收敛 分成不同的接口

```cs
public interface ITakeDamage
{
    void TakeDamage(int dmg);    // 受击
}

public interface IDropLoot
{
    void DropLoot();             // 掉落——NPC 不需要
}

public interface IPatrol
{
    void Patrol();               // 巡逻——敌人专属
}

public interface ITalk
{
    void Talk();                 // 对话——NPC 专属
}

public class Enemy : ITakeDamage IDropLoot IPatrol ITalk
{
    public void TakeDamage(int dmg) { }
    public void DropLoot() { }
    public void Patrol() { }
    public void Talk() { throw new NotImplementedException(); }
}

public class VillagerNpc : ITakeDamage ITalk
{
    public void TakeDamage(int dmg) { }
    public void DropLoot() { throw new NotImplementedException(); }
    public void Patrol() { throw new NotImplementedException(); }
    public void Talk() { }
}
```

## 依赖倒置原则
为了要兼容不同输入模块 我想要加入一个中间层 将外层输入 规范化
输出层使用规范化后的 输入
比如定义 接口 去约束输入
通过实践响应输入


## 策略模式
```cs
 public int CalcGold(string enemyRank)
{
    if (enemyRank == "Normal")       return 10;
    else if (enemyRank == "Elite")   return 50 + Random.Range(0, 20);
    else if (enemyRank == "Boss")    return 200 + Random.Range(50, 100);
    return 0;
}

interface ICalcGold{
    public int CalcGold();
}

public class NormalGold : ICalcGold{
    public int CalcGold() => return 10;
}

public class EliteGold : ICalcGold{
    public int CalcGold() => return 50 + Random.Range(0, 20);
}

public class BossGold : ICalcGold{
    public int CalcGold() => return 200 + Random.Range(50, 100);
}

public class Boss{
    ICalcGold calcGold;
    public void init(){
        calcGold = new BossGold();
    }

    public void OnDead(){
        //击杀者的金币数 += calcGold.CalcGold
    }
}
```

## 策略模式 处理多维
1. 不应该写在实体类里，如果这个实体类是通用的，我应该把赋值的这一步提取到 init的参数里 通过工厂创建
2. 感觉变得吃力了  之前的使用 不变 + 变 现在是 变 + 变 这样工作的复杂度成几何倍成长 如果 是不变 + {变，变}的话只是线性增长

## 策略模式
```cs
public interface IMoveStrategyterrain{
    float GetSpeedMultiplier(string terrain);
}

public class WalkStrategy: IMoveStrategy
{
    Dictionary<string, int> SpeedMap = new Dictionary<string, int>
    {
        ["平原"] = 1.0f, 
        ["沼泽"] = 0.5f，
        ["山地"] = 0.7f
    };
    float GetSpeedMultiplier(string terrain){
        return SpeedMap[terrain];
    }
}
public class RiderStrategy: IMoveStrategy
{
    Dictionary<string, int> SpeedMap = new Dictionary<string, int>
    {
        ["平原"] = 2.0f, 
        ["沼泽"] = 0.3f，
        ["山地"] = 0.5f
    };
    float GetSpeedMultiplier(string terrain){
        return SpeedMap[terrain];
    }
}
public class WalkStrategy: IMoveStrategy
{
    Dictionary<string, int> SpeedMap = new Dictionary<string, int>
    {
        ["平原"] = 1.5f, 
        ["沼泽"] = 1.5f，
        ["山地"] = 1.f
    };
    float GetSpeedMultiplier(string terrain){
        return SpeedMap[terrain];
    }
}

//查表
public class MoveStrategy{
    ///     平原 1.0x，沼泽 0.5x，山地 0.7x
    /// 行走
    /// 骑马
    /// 飞行

    flot[,] SpeedGroup = {
        {1,1,1},
        {1,1,1},
        {1,1,1}
    }

    // 换成枚举更明显
    float GetSpeedMultiplier(int x,int y){
        return SpeedMap[x][y];
    }

}

```

A 每个接口都要添加 三行
B 一行

A 每个接口字典夹数据
B 加一列数据

B 要更方便一些

## 观察者模式

1. 违背了开闭原则
2. 保证基础行为不依赖于其他的系统，就要保证这里不能有其他类的依赖，我的项目中通过事件总线去进行通知
3. 我只关心boss死掉的这一个瞬间 只要知道 对应死掉的 bossId就好 以及这个死亡消息

```cs
// 当前坏代码：
public class PlayerHealth
{
    public int HP;
    
    List<IOnHpChange> _handlers = new List<IOnHpChange>();
    public void Register(IOnHpChange event){
        _handlers.Add(event);
    }

    public void Register(IOnHpChange event){
        _handlers.Remove(event);
    }

    public void TakeDamage(int damage)
    {
        HP -= damage;
        
        // 直接调用各系统……
        for(int i = re.Count -1; i>=0; i++){
            re[i].OnHpChange(hp);
        }
    }
}


public class HpChangeEvent{
    long hp; 
}

interfeace IOnHpChange{
    public void OnOnHpChange(HpChangeEvent event);
}
```


## 装饰器模式 
1.  角色[元素 × 护甲 × 武器] = 不变 + {变，变，变} ， 附魔 [变+变+变+...]状态爆炸
2. 我的想法法是 这都是对装备属性的改变 那我把装备属性单独抽离处出来 提供修改接口 来进行操作
    接口 属性 * 变化 ： 属性 + 变化
    在属性中叠加同种属性变化，按照前后先后顺序复合，方便后续属性恢复

```cs
public abstract class AttackSkill
{
    public int BaseDamage;
    public abstract void Execute(Enemy target, Character owner);
    public abstract void TakeDamageBefor(Enemy target, Character owner);
    public abstract void TakeDamageAfter(Enemy target, Character owner);
    public abstract int GetDemage
}

public class NormalAttack : AttackSkill
{
    public void Execute(Enemy target, Character owner){
        TakeDamageBefor(target,owner);
        target.TakeDamge(GetDemage());
        TakeDamageAfter(target,owner);
    }

    public void TakeDamageBefor(Enemy target, Character owner){

    }
    public void TakeDamageAfter(Enemy target, Character owner){

    }
    public int GetDemage=> BaseDamage;
}

public abstract class AttackBuffDecorator  : AttackSkill
{
    protected AttackSkill addSkill;

    public AttackBuffDecorator(AttackSkill _addSkill) => addSkill = _addSkill;

    public override void Execute(Enemy target, Character owner){
        TakeDamageBefor(target,owner);
        target.TakeDamge(GetDemage());
        TakeDamageAfter(target,owner);
    }
    public override void TakeDamageBefor(Enemy target, Character owner){
        addSkill.TakeDamageBefor(target,owner);
    }
    public override void TakeDamageAfter(Enemy target, Character owner){
        addSkill.TakeDamageAfter(target,owner);
    }
    public override int GetDemage(){
        addSkill.GetDemage();
    }
}

public class CritBuff : AttackBuffDecorator { 
    public CritBuff(CritBuff _addSkill):base(_addSkill) {};
    public override void TakeDamageBefor(Enemy target, Character owner){
        addSkill.TakeDamageBefor(target,owner);
    }
    public override void TakeDamageAfter(Enemy target, Character owner){
        addSkill.TakeDamageAfter(target,owner);
    }
    public override int GetDemage(){
        int demage = addSkill.GetDemage();
        bool val = Romde(0,10)>=5;
        return val ? demage *2:demage;
        
    }
}

public class LifeStealBuff : AttackBuffDecorator {
    public LifeStealBuff(LifeStealBuff _addSkill):base(_addSkill) {};

    public override void TakeDamageBefor(Enemy target, Character owner){
        addSkill.TakeDamageBefor(target,owner);
    }

    public override void TakeDamageAfter(Enemy target, Character owner){
        addSkill.TakeDamageAfter(target,owner);
        owner.HpUp(GetDemage()*3/10);
    }

    public override int GetDemage(){
        int demage = addSkill.GetDemage();    
    }
}

public class DoubleHitBuff : AttackBuffDecorator { 
    public LifeStealBuff(LifeStealBuff _addSkill):base(_addSkill) {};

    public override void TakeDamageBefor(Enemy target, Character owner){
        addSkill.TakeDamageBefor(target,owner);
    }

    public override void TakeDamageAfter(Enemy target, Character owner){
        addSkill.TakeDamageAfter(target,owner);
        bool val = Romde(0,10)>=3;
        if(val)
            target.TakeDamage(GetDemage());
    }

    public override int GetDemage(){
        int demage = addSkill.GetDemage();    
    }
}


// 基础攻击：造成 100% 攻击力伤害
// 暴击 Buff：攻击前判断暴击率，暴击则伤害 ×2
// 吸血 Buff：攻击后恢复造成伤害的 20%
// 连击 Buff：攻击后有 30% 概率再攻击一次
```


## 工厂模式
1. 每个分支都要进行更改 骷髅兵所对应的分支
2. 我的习惯是创建一个对应 实体的管理器 根据不同状态去创建不同实现方式的实体 
    - 比如说 根据关卡不同我 进行不同的 Init(接口对应实现) 
3. 我可以创建一个阵营工厂提前配置好不同的 阵营配置 来创建不同 阵营 
    - 项目中 策划配置 对应的 阵营中有的怪物种类 不同种类对应的 属性技能 。。。 我根据这些参数来创建工厂

```cs
public abstract class SkillEvent{
    public Enemy target;
    public Enemy owner;
}

public class NormalFtrBallSkillEvent : SkillEvent{}
public class RareIcicleSkillEvent : SkillEvent{}
...不同等级不同类型技能事件


interface ISkill{
    public void Cast(Enemy target, Character owner);
}

//火球
public class NormalFireBallSkill: ISkill{
    public void Cast(Enemy target, Character owner);{
        target.TakeDamage(Owner.Atk);
        //发送事件
    }
}
public class RareFireBallSkill: ISkill{
    public void Cast(Enemy target, Character owner);{
        target.TakeDamage(Owner.Atk * 1.5);
        //发送事件
    }
}
public class LegendaryFireBallSkill: ISkill{
    public void Cast(Enemy target, Character owner);{
        target.TakeDamage(Owner.Atk * 2.0);
        target.AddBuff(new FireBuff());
    }
}

//冰锥
public class NormalIcicleSkill: ISkill{
    public void Cast(Enemy target, Character owner);{
        target.TakeDamage(Owner.Atk);
    }
}

public class RareIcicleSkill: ISkill{
    public void Cast(Enemy target, Character owner);{
        target.TakeDamage(Owner.Atk * 1.5);
    }
}

public class LegendaryIcicleSkill: ISkill{
    public void Cast(Enemy target, Character owner);{
        target.TakeDamage(Owner.Atk * 2.0);
        target.AddBuff(new IceBuff());
    }
}

//治疗
public class NormalHpUpSkill: ISkill{
    public void Cast(Enemy target, Character owner);{
        owner.HpUp(Owner.Atk);
    }
}

public class RareHpUpSkill: ISkill{
    public void Cast(Enemy target, Character owner);{
        owner.HpUp(Owner.Atk * 1.5);
    }
}

public class LegendaryHpUpSkill: ISkill{
    public void Cast(Enemy target, Character owner);{
        owner.HpUp(Owner.Atk * 2.0);
        owner.AddBuff(new HpUpBuff());
    }
}

public abstract class SkillFactory {
    public abstract ISkill Normal();
    public abstract ISkill Rare();
    public abstract ISkill Legendry(); 
}

public FireballFactory:SkillFactory {
    public override ISkill Normal() => new NormalFireBallSkill();
    public override ISkill Rare() => new RareFireBallSkill();
    public override ISkill Legendry() => new LegendryFireBallSkill();
}

public IcicleFactory:SkillFactory {
    public override ISkill Normal() => new NormalIcicleSkill();
    public override ISkill Rare() => new RareIcicleSkill();
    public override ISkill Legendry() => new LegendryIcicleSkill();
}


public HpUpFactory:SkillFactory {
    public override ISkill Normal() => new NormalHpUpSkill();
    public override ISkill Rare() => new RareHpUpkill();
    public override ISkill Legendry() => new LegendryHpUpSkill();
}

public class Enemy{
    //保护
    project int _level;
    project SkillFactory _skillFactory;
    public Init(int level, SkillFactory skillFactory) {
        _skillFactory = skillFactory;
        _level = level;
    } 

    public Cast(Enemy target, Character owner){
        ISkill skill = _level switch {
            <= 10 => _skillFactory.Normal(),
            > 10 and <=20 => _skillFactory.R(),
            < 30 => _skillFactory.L(),
            _ => _skillFactory.Normal()
        } 
    }
}

//其他附加系统触发对应的事件
音效
动画
特效
...
```

## 单例模式

1. new 的新的对象 两个系统中的 数据不同
2. 不能实现接口，mock这个不太清楚 测试 对数据的输入输出的话可以吧，他在程序开始时就已经创建
3. 丢失单例载体。 对应的坑不太清楚 可能是他引用着其他组件导致控制报错

```cs
public class AudioManager
{
    // 你来写单例骨架
    private AudioManager ins;
    public AudioManager Ins{
        get{
            if(ins == null){
                ins = new AudioManager();
            }
            return ins;
        }
    }

    public void PlaySFX(string clipName) { /* ... */ }
    public void SetMasterVolume(float v) { /* ... */ }
}

关于职责拆分
如果场景数量多参数杂 我会考虑使用通过 创建工厂管理具体实现 AudioManager 只进行策略分配
场景数大于4个，总代码数大于160行 左右考虑拆分
混响参数的话如果可以通过配置表的形式进行管理
使用 Unity 的 S...Object 这类数据管理脚本
但是我没有 具体接手对应的业务 不太了解具体业务
```

## 命令模式
1. 给所有对应组合添加一个if判断还要保证对应的优先级很麻烦，输入和表现绑定在一起 在复杂度达到一定程度时要解耦
    - 解决方法 把操作抽象为 “打字” 对输入那一串数据进行处理 再根据处理结果来进行表现
2. 撤销本质我的理解是 把我的操作后的变化 恢复到我操作前的样子， 例如我之前提的buff系统中通过 规范数值影响接口来保证我可以在结束 buff 后我的数据可以回到原本的状态

3. 战斗回放本质记录了 玩家何时做了什么操作 客户端再进行对操作命令副本 进行表现
    - 记录对应 操作时间戳 操作命令 回放时 重新计时 释放操作命令 表现效果
实现思路
输入接收

转换命令

缓存 (帧，命令)

运行命令
撤回

回放功能

## 状态模式

1. 策略模式抽象出的接口内容不依赖 主体 而状态需要依赖于主体
    - 策略模式本质是把 分支的方法抽离 保证可复用性 而这个场景主要问题并不是可复用的问题 而是内部状态增加后改动量超大的问题 随着状态增加 状态维护的复杂度陡增
2. 应该需要加入每一个判断 并增加一个case
3. 主要区别是否互相依赖 内部状态自己调用状态切换

现在给出的状态模式还是不能解决霰弹枪修改的问题么, 更换一下状态改变的顺序是不是可以 降低这个问题: 现在是在 update 触发状态切换, 那现在改成在 update 触发前 进行判断呢
单独抽离状态判断 逻辑让Update只进行 行为逻辑 

然后这个模式的处理的复杂度节点在于 不同状态之间的切换
解决这个方式是行为树 

## 外观模式
1. 违背开闭原则 不支持后续更换
2. 还要重写原本的调用甚至 调用可能更多 工作量更大

3. 给加密算法添加对应的接口 通过替换接口实现来替换
4. 适配器做兼容 接口后面要添加输入转化 
    - 外观统一对外表现, 我现在知道流程可以把每部流程转化为 参数传入接口进行统一 处理


## 模板模式
1. 技能 数量 统一流程 定义技能释放管线（类似渲染管线）
2. 策略模式更偏向于一个简单单一的小方法， 模板模式则是一个更大的抽象 有多个“策略接口” 和接口链接代码组成的一个完整管线
3. 我想的是通过定义每个接口的返回值去进行判断 最简单的是 bool，钩子方法不清楚需要你帮我讲一下

## 迭代器 组合模式
1. 创建独立与背包的 item 的迭代器  类似 itemPre -> item -> itemNext 这样需要添加一个指针
    或者单独做 item 的 数据 管理器 通过 List map 进行 储存 和系统分离开来
    毕竟数据结构本来也是一个通用的迭代器
2. 1：只去缓存 IElement 接口 使用时再去查看是否是特定组件 方便但是性能开销大
    2：比如unity 是都是gameobjct 通过绑定脚本的形式去 解耦

3. 想要迭代的话需要通用数据结构 但会有很多功能不同实现不同的组件要在一起迭代
    那我需要把他们包装到一个可以迭代 的通用数据结构中 进行管理
    但也会增加代码编写的难度把 毕竟为了通用性进行了限制

## 代理模式
1. 我的习惯是把有重复逻辑部分抽离到一个统一的管里后续修改只要修复一个地方
2. 检查逻辑是进入的必要条件 我觉得抽离到 DungeonSystem.Enter(player); 一个地方比较好
3. 能不能把这个改为判断链来避免频繁的 if else 降低这个代码的散乱程度 我的想法 
    是创建一个 类添加判断方法 返它本身
    object1.检查1(参数1... )
            .检查2(参数1... )
            .检查3(参数1... )
            .flag;

1. 场景进入管理器 保留对 条件判断类 场景进入方法 的引用
```cs
    class 场景Manager{
        private 条件判断类；
        private 场景进入实现类；

        public void Enter(Player){
            //链式条件判断
            //进入实际操作
        }
    }
    
    class T{
        //简单框架描述 
        bool flag;
        public T Start(){
            return T;
        }

        public T 判断1(参数1...){
            this.flag = //条件判断
            return this;
        }
        ...

        public bool End(){
            return flag;
        }
    }

``` 

## MVC
1. 全部逻辑放在update中 违反sop
    UI 会最快崩 最频繁的改东

    

2. UI是最长改动的 频率高到低 ui 操作 数据

3. 抽离出
    数据模块 注册数据变化事件通知 UI
    输入使用观察者 按键事件总线 抽离
    UI模块使用接口抽离
    player
        数据模块
        操作事件接收器
        UI展示接口

1. 换掉 外部操作触发器 不改动 策略模式
2. palyer 被多个 UI 关心 player只暴露一个接口 要使用 迭代模式 组合模式统一刷新 

## 代码坏味道
1. init 是什么 Update又是什么 IController 是否太抽象

1. _usePotionController  AIPlayerManager

## 重复代码
public class NormalEnemy
{
    IOnEnemyDieReward _onEnemyDieReward;
    public void Init (IOnEnemyDieReward onEnemyDieReward){
        _onEnemyDieReward = onEnemyDieReward;
    }
    
    public void OnDie(Player player)
    {
       onEnemyDieReward.OnDieReward(player);
    }
}

## 过长代码
1. 随着需求增加代码量增加后续维护压力逐渐增大
    关心了不不属于这个 方法所关心的事情 通过消息来解耦去其他系统
    三种操作 叠加在一起 基础属性变化 特定等级属性变化 满级操作

1. 
    较难找到一个合适的位置来修改这个代 

    基础属性
    阶段属性变化
    满级操作
    升级事件 整加新事件修改处

## 过长参数
1. int skillId, int level, int x, int y, int targetId, float angle, int power, string source
    结构散乱 理解困难
    需要增加新的参数时还要修改原本的参数列表
    后续维护困难

    修改参数列表 CalcDamage参数

    整合
    skillData{
        skillId，level， source,
    }
    
    castData{
        angle，x，y，power
    }

    targetData{
        targetId
    }

## 全局变量
1. 外界直接赋值 那么每次修改值的时候都需要进行判断 额外增加了复杂度
    任何一个地方都可以修改
    所有引用的地方 不管是读取还是写入
    所有给分数增加值的位置
    没有任何心里准备